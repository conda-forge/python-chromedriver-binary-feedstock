{% set name = "python-chromedriver-binary" %}
{% set version = "2.45.0" %}
{% set chromedriver_version = ".".join(version.split(".")[:2]) %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://github.com/danielkaiser/{{ name }}/archive/v{{ version }}.tar.gz
    sha256: d39f928d96e9d939dfc19fe8e4c8d18a7fb81f63940ed6a8e2b7263c1d0a7d0d
  - url: https://chromedriver.storage.googleapis.com/{{ chromedriver_version }}/chromedriver_linux64.zip  # [linux]
    sha256: d4a5eec0a3b7fec9bcb71353233dde38630e51b29fa7b218cdd196e2e4487da7  # [linux]
  - url: https://chromedriver.storage.googleapis.com/{{ chromedriver_version }}/chromedriver_max64.zip  # [osx]
    sha256: aa0f416a48e20185da62525869c1f98f994bf99f241d6ce2eb1af6ceb517c425  # [osx]
  - url: https://chromedriver.storage.googleapis.com/{{ chromedriver_version }}/chromedriver_win32.zip  # [win]
    sha256: 8f3373a260a524410e25ea643ecb0175ed49a078088c7ab4d88db323ee19a230  # [win]

build:
  number: 1001
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - setuptools

test:
  imports:
    - chromedriver_binary
    - xorg-libx11  # [linux] 
  commands:
    - chromedriver-path
    - PATH=$(chromedriver-path) chromedriver --version  # [unix]
    - PATH=$(chromedriver-path) chromedriver --help  # [unix]

about:
  home: https://github.com/danielkaiser/python-chromedriver-binary
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Downloads and installs the chromedriver binary version'
  description: |
    Downloads and installs the chromedriver binary for automated
    testing of webapps. The installer supports Linux, MacOS and Windows
    operating systems.

extra:
  recipe-maintainers:
    - igortg
    - williamjamir
    - bollwyvl
